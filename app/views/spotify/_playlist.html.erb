<li id="micropost-<%= playlist.id %>">
  <span class="user"><%= link_to playlist.user.email, playlist.user %></span>
  <span class="content"><%= playlist.playlist_obj.name %></span>
  playlist.playlist_obj.name
  <span class="timestamp">
    Posted <%= time_ago_in_words(playlist.created_at) %> ago.
  </span>
</li>
